sudo: false
language: node_js
cache:
  directories:
    - ~/.npm
    - ~/.cache
notifications:
  email: false
node_js:
  - 12.8.0
install: echo "install happening"
script: npm run setup
branches:
  only: master
